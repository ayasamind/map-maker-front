// import PinItem, { Pin } from '@/components/pinItem';
// import { Grid, Input } from '@nextui-org/react';

// export interface PinsProps {
//   pins: Pin[];
// }

// export default function Search(props: PinsProps) {
  export default function Search() {
  return (
    <div></div>
  //   <Grid.Container gap={2} wrap="nowrap" justify="center" direction="column">
  //     <Grid xs={4}>
  //       <Input clearable size="xl" placeholder="検索" type="search" css={{ width: "100%" }}/>
  //     </Grid>

  //     {props.pins.map((pin, index) => (
  //         <Grid key={index} xs={4}>
  //           <PinItem {...pin} />
  //         </Grid>
  //       ))}
  //   </Grid.Container>
  )
}
